from setuptools import setup, find_packages

setup(
    name='clear-st',
    version='0.1',
    description='A module for spatial diffusion analysis and correction',
    author='Chrissy Ma',
    packages=find_packages(),
)